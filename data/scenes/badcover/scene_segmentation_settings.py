import numpy as np


masks_points = []

#mask (Sergio)
#positive point
mask_pos_x=675
mask_pos_y=300

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Sergio/guitar)
#positive point
mask_pos_x=650
mask_pos_y=290

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Ivan)
#positive point
mask_pos_x=580
mask_pos_y=250

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Ivan/instrument)
#positive point
mask_pos_x=530
mask_pos_y=275

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Cris)
#positive point
mask_pos_x=440
mask_pos_y=320

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Cris instrument)
#positive point
mask_pos_x=460
mask_pos_y=290

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Ruben)
#positive point
mask_pos_x=290
mask_pos_y=250

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Ruben/instrument)
#positive point
mask_pos_x=270
mask_pos_y=310

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Ferran)
#positive point
mask_pos_x=400
mask_pos_y=290

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Ferran/bateria)
#positive point
mask_pos_x=400
mask_pos_y=300

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (Ferran/bateria2)
#positive point
mask_pos_x=410
mask_pos_y=390

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (David)
#positive point
mask_pos_x=100
mask_pos_y=250

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (David/instrument)
#positive point
mask_pos_x=190
mask_pos_y=315

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

#mask (David/instrument2)
#positive point
mask_pos_x=130
mask_pos_y=290

masks_points.append([np.array([[mask_pos_x, mask_pos_y]], dtype=np.float32), np.array([1], np.int32)])

